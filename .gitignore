# Build output (GitHub Actions will build this)
dist/

# Node dependencies (GitHub Actions will install)
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude session files (ignore local state, but TRACK the shared hook + settings
# so Claude Code on the web picks them up after merge to the default branch)
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json

# Legacy / deprecated files
admin-bundled.html
generate-swot-report.js
generate-swot-thai.js
migrate.sh
migrate.bat
deploy.sh
check_build.js

# Temp files
*.tmp
*.bak
